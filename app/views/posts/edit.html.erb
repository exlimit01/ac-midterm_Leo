<%= form_for @post, :url => post_path(@post), :method => :patch do |f| %>

<%= render :partial => 'form', :locals => { :f => f } %>

<p>
    <%= f.submit "更新" %>
</p>

<p><%= link_to '返回首頁', posts_path %></p>
<% end %>
